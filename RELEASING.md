# Cutting a release

Six games, three platforms, one script. `common/tools/release.sh <version>` builds every artifact the machine it runs on can build and lays them out under `release/<version>/` with a `SHA256SUMS` beside them. It is the only description of how an artifact is made. Releases are cut by hand on a machine that has the games' `gen/` and the toolchains, because a clean checkout cannot build the five recompiled titles without your own copy of each game.

    common/tools/release.sh 1.0.0                 # every title, every platform this machine can
    common/tools/release.sh 1.0.0 Vortex Lost     # only those two

## What comes out

One file per game per platform, named `<game>-<version>-<platform>.zip`, plus a source tarball.

| | |
|---|---|
| `<game>-<version>-macos.zip` | `<Game>.app`, universal (arm64 and x86_64), macOS 11 and later |
| `<game>-<version>-linux-x86_64.zip` | the executable, against the host's SDL3 and zlib |
| `<game>-<version>-windows-x64.zip` | the `.exe` and `SDL3.dll` beside it |
| `<game>-<version>-switch.zip` | the `.nro` for the Homebrew Menu (Mini Golf only, so far) |
| `<game>-<version>-android.zip` | the `.apk`, arm64 (Mini Golf only, so far) |
| `ipod-recomps-<version>-source.tar.gz` | `git archive` of the tag |

**The source tarball does not build on its own, and is not meant to.** Five of the six titles compile machine-translated code from `<title>/gen/src`, which is not in the repository and never should be: it is a mechanical translation of the original game's binary, regenerated by `tools/funcs.py` and `tools/emit.py` from the owner's own copy of that game. So a fresh tree builds Mini Golf, whose `src/game/` is the whole game by hand, and needs those two commands and a copy of the game for each of the others. Each title's README says so as the first line of its build instructions. The release notes should say it too, or somebody will report it as a bug.

**Architecture appears in a name only where it has to.** Windows is x64 alone, because Windows on ARM emulates it. macOS is one universal file rather than two downloads. The Switch is aarch64 and nothing else, and so is Android — every handheld worth playing these on is arm64, and a second ABI doubles a download to serve machines that are not out there. Linux is the one platform that genuinely needs the architecture in the name, and gets it from `uname -m` on the machine cutting the release.

**The Linux build has no glibc floor to promise, so it reports one instead.** There is no container for it, so the artifact is built against whatever glibc the release machine has and needs that version or newer. `release.sh` reads the answer out of the finished binary and prints it, and `artifact-readme.sh` puts it in the download's readme, so a player can check `ldd --version` before wondering why it will not start. Cutting a release on the oldest distribution you are willing to support is the whole of the strategy; building it in a container against an old glibc is the obvious improvement and nobody has done it.

Every artifact carries a `README.txt` written by `common/tools/artifact-readme.sh`, saying what the game's own folder is called, where it has to go on that platform, and what to do about the warning an unsigned program gets. Every artifact that contains SDL carries `SDL3-LICENSE.txt`, because SDL's licence asks to travel with it (`common/licenses/`).

## Where each part can be built

    macOS      needs a Mac: Apple's linker and install_name_tool
    Linux      needs a Linux box, with SDL3 and zlib installed
    Windows    needs only Docker — the MinGW toolchain is in a container
    Switch     needs only Docker — devkitPro is in a container
    Android    needs the NDK, the SDK's build-tools, and SDL's Android release, all of
               them no-root downloads under ~/Android (see `Mini Golf/android/README.md`)

So a Mac builds every one but Linux, and a Linux box builds every one but macOS. Anything the machine cannot build is skipped with a line saying so, and the run still succeeds; the checksums file lists what was actually made.

## The macOS build is not built against your SDL

A Homebrew SDL is built for the machine it was installed on: one architecture, and a minimum macOS equal to whatever that machine runs. An app built against it runs on Apple silicon, on the newest macOS, and nowhere else — which is fine for developing and useless for shipping. The release script downloads SDL's own release framework instead, which is universal and asks only for macOS 11, and caches it under `.release-cache/`. That is what makes the artifact universal.

## What it deliberately does not do

**It signs nothing.** An unsigned `.app` downloaded from the internet is quarantined by Gatekeeper, and an unsigned `.exe` is met by SmartScreen. Each artifact's readme tells the player how to open it anyway. Signing needs paid certificates from Apple and from a Windows certificate authority, and secrets in the repository to use them; both are decisions for whoever publishes rather than something a build script should assume. If they are ever wanted, they belong at the end of `package_macos` and `package_windows`, and nowhere else.

## Before the release

- [ ] Bump `project(... VERSION x.y.z ...)` in each title's `CMakeLists.txt`. That is the only place a version is written: from there it reaches the macOS bundle's `Info.plist` and the Switch's own metadata, both of which read `PROJECT_VERSION` rather than a literal. The version in an artifact's *name* is the argument to this script and is yours to keep in step with it.
- [ ] Tag it (`git tag v1.0.0` and push it), so the source tarball is the tag rather than whatever `HEAD` happened to be. The script says which one it used.
- [ ] Run `common/tools/release.sh 1.0.0`, then draft the release from `release/1.0.0/` (for example `gh release create v1.0.0 release/1.0.0/* --draft`). Read it before making it public.

## Adding a title, or a platform

`common/tools/titles.txt` is the list the script walks: one line per title giving its directory,
its executable, the name a person sees, the folder its game files live in, its data directory on
macOS and Windows, its data directory on Linux, and the platforms it can be built for. A new
title is a line there. A title that gains a Switch port gains `switch` on its line, and nothing
else changes.

There are two data directories because Linux spells it differently and not predictably: five
titles use `ipod-<executable>` and Mini Golf uses `ipod-mini-golf` while its executable is
`minigolf`. Each is whatever that title's `src/platform/paths.cpp` says, and the only thing that
reads them is the readme inside the artifact.

A new *platform* is a `package_<name>` function in `release.sh`, a case in the dispatch at the
bottom of it, and a case in `artifact-readme.sh` saying how a player installs it there.
